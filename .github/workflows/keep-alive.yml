name: Keep Render App Alive

on:
  schedule:
    # 定時每 10 分鐘執行一次
    - cron: '*/10 * * * *'
  workflow_dispatch: # 允許手動觸發

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Website
        run: curl -s https://momohairstyle.onrender.com > /dev/null
